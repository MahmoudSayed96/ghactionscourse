on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Choose Environment"
        required: true
        default: "development"
        type: choose
        options:
          - development
          - stage
          - production

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deployment On Dev
        run: echo 'Deployment start....'
    
